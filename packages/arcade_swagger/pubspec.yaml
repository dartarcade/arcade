name: arcade_swagger
description: Swagger support for Arcade
version: 1.5.1
repository: https://github.com/dartarcade/arcade/tree/main/packages/arcade_swagger
issue_tracker: https://github.com/dartarcade/arcade/issues
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  arcade: ^1.5.1 # Configured via catalog
  arcade_logger: ^1.5.1 # Configured via catalog
  arcade_views: ^1.5.1 # Configured via catalog
  luthor: ^0.17.0 # Configured via catalog
  openapi_spec: ^0.15.0

dev_dependencies:
  arcade_test: ^1.5.1 # Configured via catalog
  lint: ^2.8.0
  test: ^1.26.2
